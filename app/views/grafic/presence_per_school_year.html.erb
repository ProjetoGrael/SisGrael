
<div class="flex-conteiner-monitoring">
    <div class="title-monitoring">
        <h2>Presença por Periodo Letivo</h2>
        <%= link_to 'EXPORTAR PARA EXCEL', monitoring_instructor_path({}, format: :xlsx), class: "generate-button school-year-monitoring-link"%>
    </div>
    <div class="backButtonMonitoring">
        <a id="back-page" class="edit-button ">Voltar</a>    
    </div>
    <div class="card-graphic">
        <%= form_tag(:presence_per_school_year, method: :get, id: 'form_of_school_year') do  %>
            <%= select_tag(:school_year_id,options_from_collection_for_select(Academic::SchoolYear.all, "id", "name", @school_year_id),onChange: 'this.form.submit();')%> 
        <% end %>
        <%= pie_chart @date_third,  library: {pieSliceText: 'value-and-percent'}%>
    </div>
</div>

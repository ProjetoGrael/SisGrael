/*
function changeSrc(school_year_id) {
  const links = document.querySelectorAll('.school-year-monitoring-link');
  const list_of_default_link = [
      `/periodos-letivos/${school_year_id}/relatorio-de-resultado-do-conselho.pdf`,
  ]
  for (let i = 0; i < links.length; i++) links[i].href = list_of_default_link[i];

}

$(document).ready(function(){
  $('#select_school_year').change(() => {
    changeSrc($('#select_school_year').val())
  })
  changeSrc($('#select_school_year').val())
})
*/
